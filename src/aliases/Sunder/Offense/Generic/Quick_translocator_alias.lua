if snd.toggles.attacking then snd.toggle("attacking") end
if snd.toggles.bashing then snd.toggle("bashing") end
if snd.toggles.fasthunt then snd.toggle("fasthunt") end
snd.send("qeb use translocator")